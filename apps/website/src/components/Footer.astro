<footer>
  <div class="footer__container">
    <div class="footer__body">
      <div class="footer__brand">
        <img src="https://placehold.co/180x70" alt="Logo de VocesMR" />
        <p class="footer__tagline">Agencia de locución comercial en México</p>
      </div>

      <hr class="footer__divider" />

      <div class="footer__sections">
        <div class="footer__section">
          <span class="footer__eyebrow">Ubicación</span>
          <p>Valladolid 12, Roma Norte 06700</p>
          <p>Cuauhtémoc, Ciudad de México</p>
        </div>

        <div class="footer__section">
          <span class="footer__eyebrow">Contacto</span>
          <p>+52 55 3707 7261</p>
          <p>monica.reyes@vocesmr.com</p>
        </div>

        <div class="footer__section">
          <span class="footer__eyebrow">Navegación</span>
          <nav class="footer__nav">
            <a href="#">Inicio</a>
            <a href="#">Locutores</a>
            <a href="#">Proceso</a>
            <a href="#">Contáctanos</a>
          </nav>
        </div>

        <div class="footer__section voiceover">
          <span class="footer__eyebrow">¿Eres locutor?</span>
          <a href="#" class="footer__cta">¡Únete al roster!</a>
        </div>
      </div>
    </div>

    <div class="footer__legal">
      <p>&copy; 2026 VocesMR</p>
      <a href="#">Aviso de privacidad</a>
    </div>
  </div>
</footer>

<style>
  footer {
    border-top: 2px solid var(--accent-primary);
    background-color: var(--bg-primary);
  }

  .footer__container {
    display: flex;
    flex-direction: column;
  }

  .footer__body {
    padding: 2.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* ============ Brand ============ */
  .footer__brand {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .footer__brand img {
    height: 2.75rem;
    width: auto;
    object-fit: contain;
  }

  .footer__tagline {
    text-align: center;
    font-size: var(--fs-md);
    color: var(--text-muted);
    font-weight: 400;
  }

  /* ============ Divider ============ */
  .footer__divider {
    border: none;
    border-top: 2px solid color-mix(in srgb, var(--text-muted) 20%, transparent);
    margin: 0;
  }

  /* ============ Sections ============ */
  .footer__sections {
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
  }

  .footer__section {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .footer__section p {
    font-size: var(--fs-sm);
    color: var(--text-muted);
    line-height: 1.6;
  }

  .footer__nav {
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
  }

  .footer__nav a {
    font-size: var(--fs-sm);
    color: var(--text-muted);
    font-weight: 500;
    line-height: 1.8;
    transition: color var(--transition-speed) var(--transition-ease);
  }

  .footer__nav a:hover {
    color: var(--accent-primary);
  }

  .footer__nav a:active {
    color: var(--accent-primary);
  }

  /* ============ Eyebrows ============ */
  .footer__eyebrow {
    display: block;
    font-size: var(--fs-sm);
    font-weight: 600;
    letter-spacing: var(--ls-caps);
    color: var(--accent-primary);
    margin-bottom: 0.25rem;
  }

  /* ============ Voice over section ============ */
  .voiceover {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .footer__cta {
    align-items: center;
    width: fit-content;
    min-width: 10rem;
    padding: 0.7rem 1rem;
    background-color: var(--accent-primary);
    font-size: var(--fs-sm);
    color: var(--text-primary);
    text-align: center;
    font-weight: 700;
    border-radius: 8px;
    box-shadow: var(--shadow-press);
  }

  .footer__cta:hover {
    background-color: var(--accent-hover);
    transform: translateY(-1px);
    box-shadow: var(--shadow-press-hover);
  }

  .footer__cta:active {
    transform: translateY(2px);
    box-shadow: 0 1px 0 color-mix(in srgb, var(--accent-primary) 60%, #000000);
  }

  /* ============ Legal bar ============ */
  .footer__legal {
    padding: 1rem 1.5rem;
    border-top: 1px solid color-mix(in srgb, var(--text-muted) 20%, transparent);
    background-color: var(--accent-primary);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer__legal p {
    font-size: var(--fs-xs);
    color: #ebc8cb;
  }

  .footer__legal a {
    font-size: var(--fs-xs);
    color: #ebc8cb;
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color var(--transition-speed) var(--transition-ease);
  }

  .footer__legal a:hover {
    color: #f5e8e9;
  }

  .footer__legal a:active {
    color: #f5e8e9;
  }

  @media (min-width: 768px) {
    .footer__sections {
      flex-direction: row;
      justify-content: space-between;
    }

    .footer__cta {
      width: 100%;
    }
  }
</style>
